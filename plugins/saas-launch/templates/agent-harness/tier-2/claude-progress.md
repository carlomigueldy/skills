# {{PRODUCT_NAME}} — Progress

Durable memory of verified state. Read this before starting work; update it before ending a session.

## Current Verified State

Fresh scaffold. No features implemented yet. `init.sh verify` has not been run against product code — only the scaffold's own baseline (empty app, green build) is confirmed. `feature_list.json` is empty; seed it from the PRD before starting work.

## Session Records

<!--
### <date>

**Goal:** <what this session set out to do>

**Completed:**
- <change 1>
- <change 2>

**Verification evidence:**
- <command run + result, e.g. "pnpm turbo run lint typecheck build — pass">
- <Playwright/Vitest run + result>

**Features moved:**
- <feature id>: <old status> → <new status>

**Next:**
- <what the next session should do first>
-->
