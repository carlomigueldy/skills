# Copy to .env.local for local dev (see ../../supabase/config.toml — run
# `supabase start` from the repo root first, it prints these same values).

# Well-known Supabase local-dev demo keys — safe to commit, they only work
# against a local `supabase start` stack, never a hosted project.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0

# Leave blank locally; set in CI/hosting env vars for staging & prod.
VITE_SENTRY_DSN=

# PostHog product analytics (free tier). Leave blank to disable.
VITE_POSTHOG_KEY=
VITE_POSTHOG_HOST=https://us.i.posthog.com

# Contact: {{CONTACT_EMAIL}}
