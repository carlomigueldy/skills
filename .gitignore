# Dependencies
node_modules/

# OS
.DS_Store

# Logs
*.log

# Environment variables
.env
.env.*
!.env.example

# Turborepo / build output
.turbo/
dist/
.next/

# Note: the saas-launch project template ships config files only
# (no installed dependencies or build output committed) — the entries
# above mainly guard against future additions inside plugins/ and any
# scratch monorepos generated while testing the template.
