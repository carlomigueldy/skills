@AGENTS.md

# Claude Code routing

Load the relevant portable playbook from `docs/agent-harness/` before operating on a plugin. Use `roles.md` when delegating and select the requested operation from `modes.md`; these files apply equally to Claude Code and other compatible hosts.
